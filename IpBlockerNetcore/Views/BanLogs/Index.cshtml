@model IEnumerable<IpBlockerNetcore.Models.Domain.BanLog>

@{
    ViewData["Title"] = "Index";
    var duzenlemesizModel = Model.Where(x => !x.Data.Contains("rearrange"));
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(duzenlemesizModel => duzenlemesizModel.Data)
            </th>
            <th>
                @Html.DisplayNameFor(duzenlemesizModel => duzenlemesizModel.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in duzenlemesizModel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
